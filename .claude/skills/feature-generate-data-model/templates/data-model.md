---
status: Draft
owner: "<owner>"
reviewers: []
updated_at: "<YYYY-MM-DD>"
feature_size: <XS|S|M|L>
stage: "08"
ticket: "<ticket-id>"
---

# Data model — <slug>

<!-- Skill instruction: один-два абзаци. Назвати прохід — greenfield, brownfield чи
     змішаний (типовий випадок тут: частина сутностей уже існує, частина спроектована на
     потім) — і одразу дати головний висновок одним реченням: чи змінює цей прохід схему
     взагалі, чи лише фіксує форму на майбутнє. Якщо документ проєктує більше, ніж цей
     прохід мігрує (сутність відкладена на пізнішу поставку), сказати це тут явно й
     пояснити чому проєктування не можна відкласти разом з міграцією — зазвичай тому, що
     форма зберігання впливає на рішення стадії 10 (контракт API). -->

## ER diagram

<!-- Skill instruction: mermaid erDiagram, ручна розкладка (не автогенерація). Включити
     ВСІ сутності цього документа, включно з відкладеними на пізнішу поставку — розділ
     Entities нижче позначить, які з них уже змінили схему, а які лише спроектовані. -->

```mermaid
erDiagram
    <table> ||--o{ <table> : "<звʼязок людською мовою, не тільки FK-стрілка>"

    <table> {
        uuid id PK
        varchar(N) <col>
        text <col> "коментар, якщо потрібен"
        timestamptz created_at
    }
```

## Aggregate roots

<!-- Skill instruction: назвати, яка таблиця — корінь агрегату, що вона каскадом
     видаляє (on delete cascade) і чому дитина не має життя без батька. Явно сказати,
     що НЕ входить до агрегату (окремий модуль/сервіс) і чому межа проходить саме там. -->

## Entities

### `<table>` — без змін

<!-- Skill instruction: для сутності, що вже існує і цей прохід її не чіпає — короткий
     розділ без таблиці колонок: посилання на міграцію, яка її створила, і одне речення
     чому прохід її не змінює. -->

Створена міграцією `<timestamp>-<kebab>`. Цей прохід її не змінює.

### `<table>` — новий / зміна

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | UUID | PK, `default uuidv7()` | |
| `<col>` | VARCHAR(N) | NOT NULL | межа — `contracts/<feature>-limits.ts` |
| `<col>` | VARCHAR(N) | NOT NULL, CHECK IN (...) | закритий перелік — код тримає union-тип |
| `created_at` | TIMESTAMPTZ | NOT NULL DEFAULT now() | |

<!-- Skill instruction: додавати `updated_at` лише коли сутність редагується як ціле
     (як `products`); для таблиці, що фіксує події (запуск, спроба), називати момент
     своїм іменем (`started_at`, `finished_at`, `resolved_at`) замість generic
     `updated_at`. -->

**Access patterns:**
- `<запит із sad.md §6>` → індекс `<idx_name>` на `<columns>`.
- `<запит>` → **без індексу, свідомо**: `<чому — масштаб, або ilike, який B-tree не
  обслуговує>`.

**Constraints:** `<named constraint>` — `<що саме гарантує і чому це не бізнес-логіка>`.

<!-- Skill instruction: повторити ### на кожну сутність, включно з відкладеними —
     позначити заголовок "— спроектовано, поставка N" і додати рядок після таблиці:
     "Міграція — стадія/поставка N, коли [умова]." -->

## Indexes

| Index | Table | Columns | Query it serves |
|---|---|---|---|
| `<name>` | `<table>` | `<cols>` | `<query, з посиланням на сценарій sad.md §6>` |

<!-- Skill instruction: жодного рядка "про запас" — кожен індекс має конкретний запит.
     Якщо для якоїсь таблиці індексів немає взагалі (як у `products`) — сказати це тут
     одним реченням, а не залишати таблицю порожньою без пояснення. -->

## Migrations

| Change | Stage / поставка | Why |
|---|---|---|
| `<+/- table.column>` | цей прохід \| відкладено на `<stage>` | `<одне речення>` |

<!-- Skill instruction: якщо прохід не мігрує нічого — одне речення про це на початку
     розділу, і таблиця показує тільки заплановані зміни з їхньою стадією. -->

## Test fixtures

<!-- Skill instruction: назвати конкретні *.spec.ts, де живуть локальні seed-функції
     (правило 8 CLAUDE.md — окремих файлів з фабриками не заводимо). Якщо прохід не
     додає нових fixtures — сказати це і назвати, які наявні spec-файли вже покривають
     нову схему. -->

## Open items

- `<!-- TBD -->` `<питання, кому належить рішення — зазвичай наступна стадія>`
